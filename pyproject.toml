[tool.poetry]
name = "opalizer"
version = "0.1.0"
description = ""
authors = ["Ganesh Raskar <ganeshraskar@outlook.com>"]
readme = "README.md"
packages = [{include = "opalizer", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
